import { NextPage } from 'next';
import Redefine_OrderDetails from 'Templates/Redefine_OrderDetails';

const OrderDetails: NextPage = () => {
  return <Redefine_OrderDetails />;
};

export default OrderDetails;

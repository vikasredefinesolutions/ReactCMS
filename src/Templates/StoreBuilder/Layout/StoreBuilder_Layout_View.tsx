import React from 'react';

interface _props {
  children: React.ReactNode;
}

const StoreBuilder_Layout: React.FC<_props> = (children) => {
  return <>{children}</>;
};

export default StoreBuilder_Layout;

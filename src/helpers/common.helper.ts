export function removeDuplicates(arr: any[]) {
  return arr.filter(
    (arr, index, self) =>
      index === self.findIndex((t) => t.seName === arr.seName),
  );
}

// export const alertIfNoDataFound = (data) => {

// }

export const highLightResponse = ({
  dataToShow,
  component,
}: {
  dataToShow: any;
  component: string;
}) => {
  console.log(
    `|====================================================================================================================================================================> ${component}   <==RESPONSE==================================================================================================================START=>>>>`,
    dataToShow,
    `<=END================================================================================================================================================================================|`,
  );
};

export const highLightError = ({
  error,
  component,
}: {
  error: any;
  component: string;
}) => {
  console.log(
    `|====================================================================================================================================================================ERROR===ERROR===ERROR===> ${component}   <====================================================================================================================START=> ${error}     <=END================================================================================================================================================================================|`,
  );
};
